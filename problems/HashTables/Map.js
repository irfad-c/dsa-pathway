/*
A Map is a ready-made tool (in JavaScript) to use hashtable easily.
*/

const map = new Map();

map.set("name", "Irfad");
map.set("age", 26);

console.log(map.get("name"));
console.log(map.get("age"));

console.log(map);

